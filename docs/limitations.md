# Known Limitations

- Upstream receiver stack (`yt-cast-receiver`) is documented as work in progress and may not be reliable enough for strict production environments.
- End-to-end reliability depends on LAN/mDNS quality and device firmware behavior.
- Media forwarding requires URLs that are reachable by Cast targets.
